{
  "dashboard": {
    "id": null,
    "uid": null,
    "title": "${DASHBOARD_NAME}",
    "tags": ["generated"],
    "timezone": "browser",
    "schemaVersion": 36,
    "version": 0,
    "panels": [
      {
        "type": "timeseries",
        "title": "Requisições HTTP por rota",
        "gridPos": { "x": 0, "y": 0, "w": 24, "h": 10 },
        "datasource": "Prometheus",
        "targets": [
          {
            "expr": "rate(flask_http_request_total[1m])",
            "legendFormat": "{{method}} {{status}} {{path}}",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "reqps",
            "thresholds": {
              "mode": "absolute",
              "steps": [
                { "color": "green", "value": null },
                { "color": "orange", "value": 5 },
                { "color": "red", "value": 10 }
              ]
            }
          },
          "overrides": []
        },
        "options": {
          "legend": {
            "displayMode": "table",
            "placement": "bottom"
          }
        }
      }
    ]
  },
  "folderTitle": "${FOLDER_NAME}",
  "overwrite": true
}
